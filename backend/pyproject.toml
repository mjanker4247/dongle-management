[project]
name = "dongle-manager"
version = "1.0.0"
description = "Physical software dongle management system"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
pythonpath = ["."]
